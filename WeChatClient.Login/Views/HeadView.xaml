<UserControl x:Class="WeChatClient.Login.Views.HeadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WeChatClient.Login.Views"
             mc:Ignorable="d">
    <StackPanel Margin="42px">
        <Image Width="160" Height="160" Margin="50" Source="{Binding HeadImage}">
            <Image.Clip>
                <RectangleGeometry RadiusX="4" RadiusY="4" Rect="0,0,160,160"/>
            </Image.Clip>
        </Image>
        <Button Command="{Binding SwitchAccountCommand}" Width="220" Height="40" Margin="30">
            <Button.Template>
                <ControlTemplate>
                    <Border BorderThickness="1" CornerRadius="6" BorderBrush="#33000000" Cursor="Hand">
                        <TextBlock FontSize="16" Foreground="#000" VerticalAlignment="Center" HorizontalAlignment="Center">切换账号</TextBlock>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </StackPanel>
</UserControl>
